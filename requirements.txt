# Thin wrapper — canonical dependencies are in pyproject.toml.
# Use `pip install -e .` for development, or `pip install -r requirements.txt` for quick setup.
numpy>=1.24
pandas>=2.0
scipy>=1.11
opencv-python>=4.8
ultralytics>=8.0
mediapipe>=0.10
plotly>=5.18
matplotlib>=3.8
anthropic>=0.40
pyyaml>=6.0
python-dotenv>=1.0
tqdm>=4.66
PyQt6>=6.5
PyQt6-WebEngine>=6.5
pytest>=7.4
ruff>=0.1
